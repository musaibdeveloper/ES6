// Default parameters.
// Setting the parameters () & it's fixed in the function, no need to change.

function add (a =465, b = 46514){
    console.log(a*b+a-b);
}
add(); // 21582961

// There a and b values are fixed and assigned only one time.

