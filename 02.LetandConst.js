// let and Const.
// Let si used to decalre varaibles.
// In let we can re-assign the values but can't redeclare the varaible.

let name = "musaib";
console.log(name); // musaib

let name = "syed"
console.log(name); // SyntaxError: Identifier 'name' has already been declared.

// Const 
// Const has the fixed values which can't be re-declare or re-assign.

// It's fixed can't be changed. 